(() => {
  const t = /([\w-]+)|['"]{1}([^'"]*)['"]{1}/g,
    e = /<(?:"[^"]*"['"]*|'[^']*'['"]*|[^'">])+>/g,
    r = new Set(["br", "col", "hr", "img", "input", "link", "meta"]),
    n = (t, e, r, n) => {
      const o = t.slice(e, t.indexOf("<", e)).split("Þ");
      o[0].trim().length > 0 && r.appendChild(document.createTextNode(o[0]));
      for (let t = 1, e = o.length; t < e; ++t) {
        const e = document.createTextNode("Þ");
        r.appendChild(e), n.push(e), o[t].trim().length > 0 && r.appendChild(document.createTextNode(o[t]))
      }
    },
    o = (t, e) => {
      if (e && 11 == e.nodeType) t.t.parentNode.replaceChild(e, t.t);
      else if (Array.isArray(e)) {
        const {
          e: r
        } = t, {
          childNodes: n
        } = r;
        for (let t = j = 0; t < n.length && j < e.length;) {
          const o = n[t].r;
          o ? o == e[j].r ? (t++, j++) : e.some(t => t.r == o) ? r.insertBefore(e[j++], r.childNodes[t++]) : r.removeChild(r.childNodes[t]) : t++
        }
        for (; j < e.length;) r.appendChild(e[j++])
      } else t.t.nodeValue = e
    };
  StaticJS = (() => {
    let s, c;
    return (l, ..._) => {
      if (!s) {
        const {
          n: a,
          o: f
        } = (o => {
          let s = -1;
          const c = [],
            l = [];
          o.replace(e, (e, _) => {
            const a = "/" !== e[1],
              f = _ + e.length,
              i = o[f];
            let m;
            if (a) {
              let _;
              s++, ({
                name: _,
                voidElement: m
              } = (e => {
                let n, o = 0;
                const s = {
                  name: "",
                  voidElement: !1,
                  attrs: {}
                };
                return e.replace(t, t => {
                  o % 2 ? n = t : 0 === o ? ((r.has(t) || "/" === e[e.length - 2]) && (s.voidElement = !0), s.name = t) : s.attrs[n] = t.replace(/['"]/g, ""), ++o
                }), s
              })(e));
              const a = document.createElement(_),
                d = c[s - 1];
              !m && i && "<" !== i && n(o, f, a, l), d && d.append(a), c[s] = a
            }
            a && !m || (s--, "<" !== i && i && n(o, f, c[s], l))
          });
          const _ = document.createDocumentFragment();
          return _.appendChild(c[0]), {
            n: _,
            o: l.map(t => ({
              t: t,
              e: t.parentNode
            }))
          }
        })(l.join("Þ").trim());
        for (let t = 0, e = f.length; t < e; ++t) {
          const e = _[t];
          if (Array.isArray(e)) {
            f[t].t.nodeValue = "";
            const r = document.createDocumentFragment();
            for (let t = 0, n = e.length; t < n;) r.appendChild(e[t++]);
            f[t].e.appendChild(r)
          } else o(f[t], e)
        }
        return a.firstChild.r = a.r = Date.now() + Math.random(), s = f, c = _, a
      }
      for (let t = 0, e = _.length; t < e; ++t) {
        const e = _[t];
        c[t] != e && (o(s[t], e), c[t] = e)
      }
    }
  })
})();