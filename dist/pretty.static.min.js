(() => {
  const e = /([\w-]+)|['"]{1}([^'"]*)['"]{1}/g,
    t = /<(?:"[^"]*"['"]*|'[^']*'['"]*|[^'">])+>/g,
    r = new Set(["br", "col", "hr", "img", "input", "link", "meta"]),
    n = (e, t, r, n, o = e.slice(t, e.indexOf("<", t)), s = o.split("Þ")) => {
      s[0].trim().length > 0 && r.appendChild(document.createTextNode(s[0]));
      for (let e = 1, t = s.length; e < t; ++e) {
        const t = document.createTextNode("Þ");
        r.appendChild(t), n.push(t), s[e].trim().length > 0 && r.appendChild(document.createTextNode(s[e]))
      }
    },
    o = (e, t) => {
      if (t && 11 == t.nodeType) e.e.parentNode.replaceChild(t, e.e);
      else if (Array.isArray(t)) {
        const {
          t: r
        } = e, {
          childNodes: n
        } = r;
        for (let e = j = 0; e < n.length && j < t.length;) {
          const o = n[e].r;
          o ? o == t[j].r ? (e++, j++) : t.some(e => e.r == o) ? r.insertBefore(t[j++], r.childNodes[e++]) : r.removeChild(r.childNodes[e]) : e++
        }
        for (; j < t.length;) r.appendChild(t[j++])
      } else e.e.nodeValue = t
    };
  StaticJS = (() => {
    let s, c;
    return (l, ..._) => {
      if (!s) {
        const {
          n: a,
          o: f
        } = ((o, s = -1, c = [], l = []) => {
          o.replace(t, (t, _) => {
            const a = "/" !== t[1],
              f = _ + t.length,
              i = o[f];
            let m;
            if (a) {
              let _;
              s++, ({
                name: _,
                voidElement: m
              } = ((t, n = 0, o, s = {
                name: "",
                voidElement: !1,
                attrs: {}
              }) => (t.replace(e, e => {
                n % 2 ? o = e : 0 === n ? ((r.has(e) || "/" === t[t.length - 2]) && (s.voidElement = !0), s.name = e) : s.attrs[o] = e.replace(/['"]/g, ""), ++n
              }), s))(t));
              const a = document.createElement(_),
                d = c[s - 1];
              !m && i && "<" !== i && n(o, f, a, l), d && d.append(a), c[s] = a
            }
            a && !m || (s--, "<" !== i && i && n(o, f, c[s], l))
          });
          const _ = document.createDocumentFragment();
          return _.appendChild(c[0]), {
            n: _,
            o: l.map(e => ({
              e: e,
              t: e.parentNode
            }))
          }
        })(l.join("Þ").trim());
        for (let e = 0, t = f.length; e < t; ++e) {
          const t = _[e];
          if (Array.isArray(t)) {
            f[e].e.nodeValue = "";
            const r = document.createDocumentFragment();
            for (let e = 0, n = t.length; e < n;) r.appendChild(t[e++]);
            f[e].t.appendChild(r)
          } else o(f[e], t)
        }
        return a.firstChild.r = a.r = Date.now() + Math.random(), s = f, c = _, a
      }
      for (let e = 0, t = _.length; e < t; ++e) {
        const t = _[e];
        c[e] != t && (o(s[e], t), c[e] = t)
      }
    }
  })
})();